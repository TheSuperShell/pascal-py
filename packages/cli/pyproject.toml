[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Steven Rudenko", email = "srudenko@ncl.com" }
]
requires-python = ">=3.13"
dependencies = [
    "interpreter",
    "parser",
]

[project.scripts]
pascal = "cli:main"

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
parser = { workspace = true }
interpreter = { workspace = true }
