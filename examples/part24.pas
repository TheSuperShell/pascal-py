program Main;
    var some: boolean;
    var other: boolean;
begin { Main }
    if (False) Then
        some := False
    else if (10 < 3) then
        begin
            other := True
        end
    else
        other := False;
end. { Main }